local M = {
  'marko-cerovac/material.nvim',
  lazy = false,
  config = function()
    vim.g.material_style = "lighter"
    vim.cmd('colorscheme material')
    vim.keymap.set("n", "<leader>ms", "<cmd>lua require('material.functions').find_style()<CR>", {desc = "Find theme styles"})
  end
}

return M
